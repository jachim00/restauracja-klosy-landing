/** Złożony słownik PL (kanoniczny). Kolejne przestrzenie nazw dołączane w miarę ekstrakcji. */
import { common } from "./common";
import { meta } from "./meta";

export const pl = {
  common,
  meta,
};
